# Rollback / cutover for Headless viewer links (default off in repo docs)
PUBLIC_USE_HEADLESS_VIEWER=false
# Optional absolute preview origin when flag is on:
# PUBLIC_HEADLESS_VIEWER_BASE=https://YOUR-PREFIX-noam-math-astro-poc-amiramnoam-130a.wix-site-host.com
