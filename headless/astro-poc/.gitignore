# build output
dist/
# generated types
.astro/

# dependencies
# (bare name too: in this worktree node_modules is a SYMLINK to the shared
# install, and a trailing-slash rule matches real directories only)
node_modules/
node_modules

# Wix CLI / local hosting metadata
.wix/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
wix-error.log
*.log

# environment variables & secrets (never commit)
.env
.env.*
!.env.viewer.example
!.env*.example

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# local reports / QA dumps
reports/
